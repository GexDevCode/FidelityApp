import { StyleSheet } from "react-native";
import { Colors } from "../constants/colors";

const styles = StyleSheet.create({
    input: { width: "100%", marginTop: 20, backgroundColor: Colors.greyInput, borderRadius: 18, paddingHorizontal: 20, color: Colors.black }
})

const StyleInput = styles;
export default StyleInput;